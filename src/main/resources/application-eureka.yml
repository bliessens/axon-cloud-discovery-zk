axon:
  distributed:
    enabled: true
    spring-cloud:
      fallback-url: /message-routing-information
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
spring:
  cloud:
    zookeeper:
      enabled: false